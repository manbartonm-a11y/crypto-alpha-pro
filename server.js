const express = require('express');
const app = express();

app.get('/', (req, res) => {
  res.send('OK');
});

app.get('/telegram', (req, res) => {
  res.write('<!DOCTYPE html>');
  res.write('<html>');
  res.write('<head>');
  res.write('<meta charset="utf-8">');
  res.write('<meta name="viewport" content="width=device-width,initial-scale=1">');
  res.write('<title>Crypto Alpha Pro</title>');
  res.write('<style>');
  res.write('body{margin:0;background:#000;color:#0f0;font-family:monospace;text-align:center;padding:20px}');
  res.write('h1{color:#0ff;font-size:3.5em}');
  res.write('.p{font-size:5.5em;color:#0f9;margin:10px}');
  res.write('canvas{width:95%;max-width:600px;height:280px;border:6px solid #0f0;border-radius:20px;margin:30px auto;background:#000}');
  res.write('.live{color:#0f0;animation:glow 1.5s infinite alternate}@keyframes glow{from{text-shadow:0 0 20px #0f0}to{text-shadow:0 0 60px #0f0}}');
  res.write('</style>');
  res.write('</head>');
  res.write('<body>');
  res.write('<h1>CRYPTO ALPHA PRO</h1>');
  res.write('<div class="p" id="price">,733</div>');
  res.write('<div id="change" style="font-size:2em;color:#0f0">24h -0.36%</div>');
  res.write('<canvas id="c"></canvas>');
  res.write('<div id="whale-box" class="live" style="background:#001a00;padding:20px;border:3px solid #0f0;border-radius:20px;margin:20px;font-size:1.5em">WHALE ALERT Loading...</div>');
  res.write('<div style="font-size:1.7em;color:#0f9;margin-top:20px">Whales buying the dip — next leg up loading</div>');
  res.write('<script>');
  res.write('const canvas = document.getElementById("c");');
  res.write('const ctx = canvas.getContext("2d");');
  res.write('canvas.width = 600; canvas.height = 280;');
  res.write('ctx.fillStyle = "#000"; ctx.fillRect(0, 0, 600, 280);');
  res.write('ctx.strokeStyle = "#0f0"; ctx.lineWidth = 8; ctx.beginPath();');
  res.write('ctx.moveTo(0, 250); ctx.lineTo(50, 230); ctx.lineTo(100, 220); ctx.lineTo(150, 180); ctx.lineTo(200, 200); ctx.lineTo(250, 160); ctx.lineTo(300, 140); ctx.lineTo(350, 120); ctx.lineTo(400, 100); ctx.lineTo(450, 80); ctx.lineTo(500, 60); ctx.lineTo(550, 40); ctx.lineTo(600, 30);');
  res.write('ctx.stroke(); ctx.fillStyle = "rgba(0,255,0,0.3)"; ctx.fill();');
  res.write('setInterval(() => {');
  res.write('fetch("https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=usd&include_24hr_change=true")');
  res.write('.then(r => r.json())');
  res.write('.then(d => {');
  res.write('document.getElementById("price").innerText = "$" + Number(d.bitcoin.usd).toLocaleString();');
  res.write('const ch = d.bitcoin.usd_24h_change.toFixed(2);');
  res.write('document.getElementById("change").innerHTML = "24h " + (ch > 0 ? "<span style=\"color:#0f0\">Up +" + ch + "%</span>" : "<span style=\"color:#f66\">Down " + Math.abs(ch) + "%</span>");');
  res.write('});');
  res.write('}, 5000);');
  res.write('setInterval(() => {');
  res.write('fetch("https://api.whale-alert.io/v1/transactions?min_value=1000000&currency=btc,eth,usdt&limit=1")');
  res.write('.then(r => r.json())');
  res.write('.then(d => {');
  res.write('if (d.transactions && d.transactions[0]) {');
  res.write('const t = d.transactions[0];');
  res.write('const amount = (t.amount_usd / 1000000).toFixed(1);');
  res.write('const dest = t.to.owner ? t.to.owner : t.to.address.slice(0,10) + "...";');
  res.write('document.getElementById("whale-box").innerHTML = "WHALE ALERT $" + amount + "M " + t.symbol.toUpperCase() + " ? " + dest + " <small>(just now)</small>";');
  res.write('} else {');
  res.write('document.getElementById("whale-box").innerHTML = "WHALE ALERT No recent moves";');
  res.write('}');
  res.write('});');
  res.write('}, 30000);');
  res.write('</script>');
  res.write('</body>');
  res.write('</html>');
  res.end();
});

const PORT = process.env.PORT || 10000;
app.listen(PORT, '0.0.0.0', () => {
  console.log('Crypto Alpha Pro LIVE on Render!');
});
