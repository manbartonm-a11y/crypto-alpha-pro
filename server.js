const express = require("express"); const fetch = require("node-fetch"); const app = express(); app.get("/", (req, res) => res.send("<h1 style=\"color:#0f0;background:#000;text-align:center;padding:200px;font-family:monospace\">CRYPTO ALPHA PRO LIVE!<br><a href=\"/telegram\" style=\"color:#0ff;font-size:50px\">OPEN DASHBOARD</a></h1>")); app.get("/telegram", async (req, res) => { let price = "Loading..."; let change = ""; try { const data = await fetch("https://api.coingecko.com/api/v3/simple/price?ids=bitcoin&vs_currencies=usd&include_24hr_change=true").then(r => r.json()); price = "$" + Number(data.bitcoin.usd).toLocaleString(); change = data.bitcoin.usd_24h_change.toFixed(2); change = change > 0 ? `<span style=\"color:#0f0\">Up +${change}%</span>` : `<span style=\"color:#f66\">Down ${Math.abs(change)}%</span>`; } catch(e) { change = "API limited"; } res.send(`<!DOCTYPE html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Crypto Alpha Pro</title><style>body{margin:0;background:#000;color:#0f0;font-family:monospace;padding:40px;text-align:center}h1{color:#0ff;font-size:3em}.p{font-size:6em;margin:30px}</style><script src="https://telegram.org/js/telegram-web-app.js"></script></head><body onload="Telegram.WebApp.ready();Telegram.WebApp.expand()"><h1>CRYPTO ALPHA PRO</h1><div class="p">${price}</div><div style="font-size:2em">24h ${change}</div><div style="margin-top:50px;font-size:1.8em;color:#0f9">Bitcoin whales are buying the dip — next leg up loading.</div></body></html>`); }); const PORT = process.env.PORT || 10000; app.listen(PORT, "0.0.0.0", () => console.log("LIVE WITH REAL PRICE"));
